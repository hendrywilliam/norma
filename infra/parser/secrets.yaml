apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: parser-secrets
  namespace: norma
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: prod-secret-store
  target:
    name: parser-secrets
    creationPolicy: Owner
  # data:
  #   - secretKey: DB_PASSWORD
  #     remoteRef:
  #       key: PLACEHOLDER: /norma/production/DB_PASSWORD
  #   - secretKey: GLM_API_KEY
  #     remoteRef:
  #       key: PLACEHOLDER: /norma/production/GLM_API_KEY
