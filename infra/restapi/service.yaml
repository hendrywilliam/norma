apiVersion: v1
kind: Service
metadata:
  name: restapi-service
  namespace: norma
  labels:
    app: restapi
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: restapi